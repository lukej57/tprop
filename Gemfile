# frozen_string_literal: true

source "https://rubygems.org"

# Depend on the gemspec for runtime dependencies.
gemspec

group :development, :test do
  gem "minitest", "~> 5.0"
  gem "rake", "~> 13.0"
end
